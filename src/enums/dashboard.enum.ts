export enum DashboardBreakdownBy {
  COUNTRY = 'country',
  FLAVOR = 'flavor',
  PRICE_BUCKET = 'priceBucket',
  STORE = 'store',
  TYPE = 'type',
}

export enum DashboardGranularity {
  DAY = 'day',
  WEEK = 'week',
}
