.git
.idea
node_modules
dist
.env*
!.env.dev
*.dump
*.db
*.log
# Deployment config the image never runs: mounted into the `lb` container
# from the host. Excluded so editing it does not invalidate the build cache.
haproxy.cfg
